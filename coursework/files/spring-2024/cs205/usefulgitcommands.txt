git log --pretty=format:"%h %s" --graph

git log -- path/to/file

git log -S function_name - if you wanted to find the last commit that added or removed a reference to a
specific function, 

git log --stat

git commit -m "Story 182: fix benchmarks for speed"

git add

git rm

 If you want to redo that commit
git commit --amend

As an example, if you commit and then realize you forgot to stage the changes in a file you wanted
to add to this commit, you can do something like this:
$ git commit -m 'Initial commit'
$ git add forgotten_file
46
$ git commit --amend

Unstaging a Staged File

git reset HEAD CONTRIBUTING.md

commit [ID]: This is the ID number of the committed change to the repository. The top commit in the output is the most recent and the subsequent commits in the listed output are made further and further back in time until you get to the very first, Initial Commit.

Author: Dieter Otte <wolfdieterotte@gmail.com> : The name and email of the commit's author. Note that there can be multiple different authors who have made a commit to a repository or just a single author, but this information is recorded for each commit.

Date: Mon Nov 4 22:52:08 2024 +0000 : The date when the change was committed to the repository.

Changed WTS Header File  : The commit message that was submitted with the call to git commit. 